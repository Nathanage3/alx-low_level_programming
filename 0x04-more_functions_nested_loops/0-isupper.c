#include "main.h"

/**
 * _isupper - Checks the uppercase characters
 * @c: The character to be executed
 * Return: Always 1 for uppercase and 0 otherwise
 */


int _isupper(int c)
{

	if (c >= 65 && c <= 90)
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
