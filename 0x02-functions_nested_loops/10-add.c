#include "main.h"
/**
 * add - adding numbers
 * @c: the parameter to be tested
 * @d: the second parameter to be tested
 * Return: Always 0 for success and 1 for failure
 */
int add(int c, int d)
{
	int c, d;

	return (c + d);
}
