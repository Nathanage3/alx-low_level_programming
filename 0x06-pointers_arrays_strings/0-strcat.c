#include "main.h"
#include <string.h>
/**
 * *_strcat - string concatinate
 * @dest: char destination
 * @src: char pointer
 * Return: Always 0
 */

char *_strcat(char *dest, char *src)
{
	const char* a[98];
       
	const char* [98];


	const char* p[100];

	 strcpy(p[100], a[98]);
	 strcat(p[100], b[98]);
	return strcat;
}
